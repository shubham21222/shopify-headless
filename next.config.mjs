/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.shopify.com', 'picsum.photos','images.unsplash.com','images.ctfassets.net'],
    },
};

export default nextConfig;
